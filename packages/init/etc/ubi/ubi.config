# This is the UBI configuration file.  It influences the behavior of the ubi
# initialization and the usbctl.

# This is a list of configuration words for mtd devices owned by the UBI layer.
# Each configuration word is of the form MTD_NAME:UBI_MOUNTPOINT:FAT_VOLUME_NAME
# The FAT_VOLUME_NAME is only used when formatting the devices
UBI_MTD_CFGS="Brio:/Didj:Didj prg_Brio:/opt/Didj:Didj Cartridge:/Cart:Cartridge EXT:/opt/mfg:MFG_PAYLOAD prg_EXT:/opt/prg_mfg:MFG_PAYLOAD"
